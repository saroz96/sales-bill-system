// const generatePrefix = () => {
//     // Define a list of possible prefixes
//     const prefixes = ['AASR', 'ABSR', 'BASR', 'CASR', 'DASR']; // Extend this list as needed

//     // Randomly select a prefix from the list
//     const randomIndex = Math.floor(Math.random() * prefixes.length);
//     return prefixes[randomIndex];
// };

// module.exports = { generatePrefix };