<!-- <%- include('../layouts/boilerplate') %>

    <div class="container">
        <h1 class="mt-5">Stock Adjustments for <%= item.name %>
        </h1>

        <h2 class="mt-5">Current Stock: <%= item.stock %>
        </h2>

        <form action="/items/<%= item._id %>/stock-adjustments" method="post" class="mt-4">
            <div class="form-group">
                <label for="type">Adjustment Type</label>
                <select class="form-control" id="type" name="type" required>
                    <option value="excess">Excess</option>
                    <option value="short">Short</option>
                </select>
            </div>
            <div class="form-group">
                <label for="quantity">Quantity</label>
                <input type="number" class="form-control" id="quantity" name="quantity" placeholder="Enter quantity"
                    required>
            </div>
            <div class="form-group">
                <label for="reason">Reason</label>
                <input type="text" class="form-control" id="reason" name="reason" placeholder="Enter reason" required>
            </div>
            <button type="submit" class="btn btn-primary">Add Adjustment</button>
        </form>

        <h2 class="mt-5">Stock Adjustments History</h2>
        <ul class="list-group mt-3">
            <% if (item.stockAdjustments && item.stockAdjustments.length> 0) { %>
                <% item.stockAdjustments.forEach(adjustment=> { %>
                    <li class="list-group-item">
                        <strong>
                            <%= adjustment.type %>:
                        </strong>
                        <%= adjustment.quantity %> on <%= adjustment.date.toDateString() %> - <%= adjustment.reason %>
                    </li>
                    <% }); %>
                        <% } else { %>
                            <li class="list-group-item">No stock adjustments</li>
                            <% } %>
        </ul>

        <a href="/items" class="btn btn-link mt-4">Back to Items</a>
    </div> -->